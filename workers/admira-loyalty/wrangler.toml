name = "admira-loyalty"
main = "src/index.js"
compatibility_date = "2025-12-01"

[vars]
ALLOWED_ORIGINS = "https://csilvasantin.github.io,http://localhost:9124,http://127.0.0.1:9124,http://localhost:5173"
JOIN_CODE = "XTANCO26"
ACTIVE_WINDOW_SECONDS = "120"
STAMPS_FOR_FREE = "5"
# Gamification bridge: 'mock' simulates Open Loyalty inside the worker (default,
# safe for the prod PoC). Set to 'live' + the OL_* secrets to hit OL cloud.
OL_MODE = "mock"

# Drain the Open Loyalty outbox every 5 minutes.
[triggers]
crons = ["*/5 * * * *"]

[[d1_databases]]
binding = "DB"
database_name = "admira-loyalty-db"
database_id = "f56fb3b0-584f-4bfb-9d28-7a13e26b5088"
